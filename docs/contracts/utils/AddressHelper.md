# AddressHelper

This file is a legacy placeholder.

There is no standalone `AddressHelper` utility module in the active Cairo contracts under `src/`.

Address handling in the current codebase uses Cairo-native `ContractAddress` values and Starknet/OpenZeppelin utilities directly inside the contracts.

## Relevant Sources

- `src/nft/nft.cairo`
- `src/nftfactory/nftfactory.cairo`
- `src/receiver/receiver.cairo`
